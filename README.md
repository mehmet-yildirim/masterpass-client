# Masterpass Client

## Prerequisities

Java JDK 11+ and Maven should be installed and verified for correct installation.

## Usage
Checkout the project and run:

    mvn clean install
    ./run

Browse the app via http://localhost:8585 

Port number can be changed by changing `--server.port` variable in `run.bat` file to desired port.
